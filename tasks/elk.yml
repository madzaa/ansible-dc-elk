---
- name: deployment
  hosts: localhost
  tasks:
    - docker_service:
        project_src: ../dc-elk/
      register: output

    - docker_service:
        project_src: ../dc-elk/
        build: no
      register: output

    - docker_service:
        project_src: ../dc-elk/
        build: no
        restarted: true
      register: output
